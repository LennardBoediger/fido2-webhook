#  .pre
#  build
#  test
#  deploy
#  .post

build:
  tags:
    - ci
  stage: build
  script:
    - sudo make ci
  artifacts:
      paths:
      - CI_COMMIT_SHA.hex

  # CI_BUILDS_DIR The top-level directory where builds are executed. ?